# Owned by Flyway (PORT.md Phase 2). The first real schema migration lands there;
# until then this directory stays empty so the Compose `migrate` wiring can be
# exercised without claiming any ledger schema exists.
